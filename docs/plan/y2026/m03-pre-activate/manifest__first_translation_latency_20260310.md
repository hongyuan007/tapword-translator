# Code Change Handoff Manifest

## 1. Change Context

- **Related Documents**:
  - `docs/plan/y2026/m03-pre-activate/README.md`
  - `docs/plan/y2026/m03-pre-activate/logs/content脚本日志.txt`
  - `docs/plan/y2026/m03-pre-activate/logs/后台脚本日志.txt`
  - `docs/plan/y2026/m03-pre-activate/first-translation-latency-plan.md`
  - `docs/prompt_files/review/generate-review-manifest.prompt.md`
- **Task Objectives**:
  - Attempted to reduce first-translation cold-start latency in the extension.
  - Adjusted background startup logic intending to separate critical readiness from non-critical warm-up work.
  - Modified token warm-up behavior intending to make `PAGE_ACTIVATED` prefetch deterministic instead of race-prone.
  - Added session-scoped JWT persistence intending to reduce repeated authentication after MV3 service worker restarts.
  - Adjusted config and quota initialization intending to move more storage/network work off the first translation request path.

## 2. File Change Audit

| File Path | Change Type | Objective Description |
| :--- | :--- | :--- |
| `src/2_background/index.ts` | Mod | Adjusted startup flow to register listeners, begin critical initialization, and defer non-critical warm-up/token fetch work off the blocking initialization path |
| `src/2_background/services/ServiceInitializer.ts` | Mod | Split background initialization into critical readiness and background warm-up phases; added shared readiness/warm-up promises to deduplicate startup work |
| `src/2_background/handlers/TokenWarmUpHandler.ts` | Mod | Changed `PAGE_ACTIVATED` handling to respond immediately but still await critical readiness and then attempt token warm-up |
| `src/2_background/handlers/TranslationRequestHandler.ts` | Mod | Added critical readiness gating before serving translation requests and triggered background warm-up from the request path |
| `src/2_background/handlers/FragmentTranslationRequestHandler.ts` | Mod | Added the same readiness gating/warm-up behavior for fragment translation requests |
| `src/2_background/handlers/SpeechSynthesisRequestHandler.ts` | Mod | Added the same readiness gating/warm-up behavior for speech synthesis requests |
| `src/5_backend/services/AuthService.ts` | Mod | Added session-backed JWT restore/persist/clear logic using `chrome.storage.session`; attempted to reuse valid tokens across service worker restarts and clear them on auth-context mismatch or failure |
| `src/5_backend/services/ConfigService.ts` | Mod | Changed config initialization so cached/default config becomes available first and the initial cloud fetch runs in the background instead of blocking initialization |
| `src/5_backend/services/QuotaManager.ts` | Mod | Added deduplicated lazy initialization and explicit `ensureInitialized()` calls so quota reads/writes can self-initialize safely without assuming startup already completed |
| `docs/plan/y2026/m03-pre-activate/first-translation-latency-plan.md` | Add | Added the analysis/implementation plan used to justify the cold-start optimization work |

## 3. AI Generation Disclaimer & Risk Warnings (AI Disclaimer)

> **Important Note for Reviewer**:
> The code in this submission was generated by an AI assistant based on documentation. **Do not assume the code logic is correct.**

You need to prioritize reviewing the following potential risk points (based on the logic of this modification):
- [ ] **Logical Consistency**: Check whether the new “critical readiness vs warm-up” split actually preserves the previous initialization guarantees for translation, fragment translation, and speech paths.
- [ ] **Side Effects**: Check whether moving config fetch to background execution changes popup/version/quota behavior on a true cold start or after extension update.
- [ ] **Edge Cases**: Test service worker restart, tab activation before auth init, multiple concurrent first requests, and missing/expired/invalid session token states.
- [ ] **Authentication Logic**: Verify that `chrome.storage.session` token reuse cannot preserve a token across mismatched `uid` or `baseURL`, and confirm token clearing behavior on failures is not too aggressive.
- [ ] **Quota Integrity**: Verify lazy quota initialization does not double-initialize or lose counts when first translation and background warm-up run concurrently.
- [ ] **MV3 Lifecycle Behavior**: Verify that the new warm-up approach behaves correctly when the service worker is suspended/resumed and does not rely on promises that become invalid across worker restarts.
