# Code Change Handoff Manifest

## 1. Change Context

- **Related Documents**:
  - `docs/plan/y2026/m03-full-translate/progress.md`
  - `docs/plan/y2026/m03-full-translate/requirements.md`
  - `src/11_full_translate/README.md`
  - `translate-api/src/7_generate/services/FullTextBatchTranslation.service.ts`
- **Task Objectives**:
  - This task attempted to optimize the full-text translation backend to handle content moderation failures at the segment level rather than failing the entire batch. When the XML batch approach fails (e.g., due to one segment triggering content moderation), the service now automatically falls back to translating each text individually, isolating failures to specific segments while allowing other segments to succeed.
- **Artifact Directory**: `docs/plan/y2026/m03-full-translate/review/round-local/`

## 2. File Change Audit

| File Path | Change Type | Objective Description |
| :--- | :--- | :--- |
| `/Users/hongyuan/project/translate-api/src/7_generate/services/FullTextBatchTranslation.service.ts` | Mod | Added `IndividualTranslationFallback` type definition and optional `fallbackTranslator` parameter to constructor. Modified `translateBatch()` to catch LLM call failures and invoke `fallbackToIndividual()` method. Added private `fallbackToIndividual()` method that uses `Promise.allSettled()` to translate each text individually, returning empty strings for failed segments. Updated factory function to accept fallback parameter. |
| `/Users/hongyuan/project/translate-api/src/1_translate/services/translation.service.ts` | Mod | Modified `getFullTextBatchTranslationServiceInstance()` to create and pass a `fallbackTranslator` function that calls `translateFragment()` for individual translations. The fallback returns empty string and null usage on failure. Simplified `translateFullTextBatch()` by removing outer try-catch fallback logic since the service now handles fallback internally. Removed `translateFullTextBatchFallback()` function as it is no longer used. |

## 3. Artifact Index

- PR metadata: `pr-metadata.json` (if applicable)
- Diff: `pr-diff.patch` (if applicable)
- CI/checks: `ci-checks.txt` (if applicable)
- Subreviews: `subreviews/*.md` (if applicable)
- Final review: `final-review.md` (if applicable)
- Summary: `review-summary.md` (if applicable)

## 4. AI Generation Disclaimer & Risk Warnings (AI Disclaimer)

> **Important Note for Reviewer**:
> The code in this submission was generated by an AI assistant based on documentation and local code inspection. **Do not assume the code logic is correct.**

You need to prioritize reviewing the following potential risk points:

- [ ] **Logical Consistency**: Verify that the fallback behavior is correct when XML batch fails. Confirm that `Promise.allSettled()` properly isolates individual segment failures without affecting successful segments.
- [ ] **Side Effects**: Check whether the fallback logic could cause unexpected API rate limiting or token usage patterns. Individual translations may consume more quota than batch translation.
- [ ] **Edge Cases**: Verify behavior when all segments fail (all empty strings returned), when fallback itself throws errors, and when `fallbackTranslator` is `undefined`.
- [ ] **Frontend Contract**: Confirm that empty strings returned by the backend are correctly handled by the frontend (`PageTranslationManager.ts` check `if (translated)` should prevent insertion of empty translations).
- [ ] **Token Usage Aggregation**: Verify that token usage is correctly aggregated when some segments succeed and others fail in the fallback path.
- [ ] **TypeScript `exactOptionalPropertyTypes` Compliance**: The conditional assignment `if (fallbackTranslator) { this.fallbackTranslator = fallbackTranslator }` was added to satisfy strict optional property types. Verify this pattern is correct for the codebase.
- [ ] **Error Propagation**: Ensure that when `fallbackTranslator` is not provided and XML batch fails, the error is properly re-thrown and propagated to the caller.
- [ ] **Concurrency**: Verify that `Promise.allSettled()` in `fallbackToIndividual()` correctly handles concurrent individual translation requests without race conditions.
- [ ] **Logging**: Check that log messages accurately reflect the fallback behavior and that failure counts are correctly reported.
- [ ] **Performance**: The fallback makes N individual API calls instead of 1 batch call. Verify this is acceptable for the expected batch sizes and rate limits.
